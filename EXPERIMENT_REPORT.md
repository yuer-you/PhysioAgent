# PhysioAgent 统一实验报告

## 1. 项目目标

PhysioAgent 研究一个明确、可评测的问题：小型开源大模型能否根据中文或英文请求，生成严格的生理时序工具计划，并把计划交给确定性 Python 工具在真实 ECG 上执行。

模型不直接判断疾病，也不直接生成心率数值。它只负责选择工具、排列步骤和填写用户明确给出的参数；所有数值来自工具返回结果。本项目仅用于学习和软件评测，不用于临床诊断。

## 2. 系统结构

```text
用户问题 + signal_profile
  -> Qwen2.5-3B / LoRA SFT / DPO planner
  -> 严格 {"steps":[...]} JSON 校验
  -> WorkflowExecutor 状态传递
  -> load / filter / statistics / peaks / heart-rate tools
  -> 工具结果摘要与 grounded answer
  -> 原始计划、执行轨迹和停止原因落盘
```

工具层包含 `load_signal`、`filter_signal`、`calculate_statistics`、`detect_peaks` 和 `calculate_heart_rate`。ECG profile 使用冻结的 `ecg_detector_v1`；通用时序峰检测器与 ECG 检测器分开，避免把文本模型错误和信号算法错误混在一起。

## 3. 后训练路线

### 单工具 SFT

早期任务只输出一个工具调用。冻结的 100 条最终测试上：

| 配置 | 严格完全匹配 |
|---|---:|
| 原始 Qwen + Prompt v4 | 90 / 100 |
| 单工具 LoRA v1 | 80 / 100 |
| 单工具 LoRA v2 | 95 / 100 |

LoRA v2 的 95% 相比 Prompt v4 的 90% 在该样本量下未达到统计显著，因此报告为“本测试集提升 5 个百分点”，不外推为普遍提升。

### 多步 Workflow SFT

基础 Qwen 在 final v1 中暴露出遗漏加载、错误列名和三步计划退化。项目随后独立生成 Workflow SFT v2：1800 条训练、300 条验证，训练/验证加载释义和显式列名隔离，并包含“不应加载”的对照样例。

单张 RTX A4000 上训练 2 epoch，耗时约 32 分 33 秒；可训练参数 3.69M，占 3B 模型约 0.119%。在同一 final v3 上：

| 配置 | 严格端到端 | 有效计划 |
|---|---:|---:|
| 基础 Qwen | 25 / 60（41.7%） | 93.3% |
| Workflow LoRA v2 | 36 / 60（60.0%） | 100% |

逐例修复 12 条、回退 1 条，双侧精确检验约 `p=0.0034`。主要剩余问题是三步计划遗漏开头的 `load_signal`。

### Workflow DPO

DPO v1 从 Workflow LoRA v2 继续训练，reference policy 是训练开始前的冻结 SFT adapter。1000/200 对偏好数据覆盖遗漏加载、默认列虚构、显式列丢失、擅自添加加载、遗漏最终分析和重复最终工具。chosen 更长、等长、较短的训练对为 380/320/300，降低长度捷径。

单张 RTX A4000 训练 1 epoch，耗时约 20 分 11 秒。验证偏好准确率 99.5%，但 chosen 与 rejected reward 都为负，因此仍以自主生成评测为准。

同一 final v4 上：

| 配置 | 严格端到端 | 单步 | 两步 | 三步 |
|---|---:|---:|---:|---:|
| Workflow LoRA v2 | 63 / 80（78.8%） | 10 / 10 | 21 / 28 | 32 / 42 |
| Workflow DPO v1 | 66 / 80（82.5%） | 10 / 10 | 23 / 28 | 33 / 42 |

DPO 修复 3 条、回退 0 条，绝对提升 3.75 个百分点；配对精确检验约 `p=0.25`。正确结论是“小幅、方向正确但未达到统计显著的改善”。final v3 与 final v4 使用不同问题，不能直接用两者绝对分数比较模型阶段。

## 4. 真实 ECG 工具评测

数据使用 MIT-BIH 记录 100、101、200、207 的第 0 通道前 30 秒，采样率 360 Hz。划分在算法修改前固定：100 为开发，101/200 为验证，207 为一次性冻结测试。

通用峰检测基线在开发+验证的 115 个专家心搏上 micro F1 为 0.622，平均心率绝对误差 73.84 BPM，主要把 T 波误认为额外心搏。冻结的 `ecg_detector_v1` 在所选开发+验证片段上匹配 115/115，在记录 207 上匹配 29/29，心率绝对误差 0 BPM。

这些结果仅证明轻量算法能处理所选四段数据，不代表跨患者、导联、设备或噪声泛化。20 条真实 ECG Agent 集成案例为 20/20，但它们属于接口验收，不是新的未见模型测试。

## 5. 数据与评测治理

- 每次模型迭代前冻结新的 final 文件及 SHA-256；
- train/validation/test 问题做精确去重；
- 最终测试失败只用于下一阶段错误分类，不复制原问题进入训练；
- Base、SFT、DPO 对照使用相同提示词、解码预算和严格解析器；
- 严格指标与保守恢复指标分开；最终主结果不使用恢复；
- 模型计划、工具执行、参考检查和回答依据分别计分；
- 所有结果保存在项目 `outputs/` 下。

关键冻结哈希和完整命令见 [复现指南](docs/REPRODUCIBILITY.md)。

## 6. 失败演化

```text
基础模型：JSON 闭合、虚构参数、遗漏加载、三步退化
  -> SFT：JSON 与 schema 稳定，单步/两步明显提升
  -> DPO：轻微修复加载完整性，无逐例回退
  -> 剩余：普通描述词被当列名、显式列参数名变形、少量漏步骤/错最终工具
```

DPO final v4 的 14 条失败包括 10 条参数错误、2 条步骤数量错误、1 条工具序列错误和 1 条 schema 错误。没有生成长度截断。

## 7. 资源与工程取舍

- 模型：Qwen2.5-3B-Instruct；
- 训练：单张 16GB RTX A4000；
- 精度：FP16；
- LoRA：`r=16`、`alpha=32`、目标 `q_proj/v_proj`；
- SFT 有效 batch size 16，最大长度 1024；
- DPO policy/reference 共享基础模型，通过两个 adapter 切换，避免复制 3B 权重；
- vLLM、PPO/GRPO、多机分布式和复杂 Agent 框架不在本项目主线内。

## 8. 结论

项目完成了从 CPU 工具、规则 Agent、Qwen 基线、LoRA SFT、真实 ECG 执行、多步 Workflow 到 DPO 和冻结配对评测的完整链路。最有价值的结果不是某个单一分数，而是把数据构造、训练、严格解析、工具执行、统计比较和失败分析做成了可复现闭环。

当前最优交付模型为 Workflow DPO v1，但其相对 SFT 的最终提升有限。项目保留这一负责任的结论，并停止针对已解封 final v4 继续调参。
